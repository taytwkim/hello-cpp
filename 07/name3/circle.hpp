#ifndef CIRCLE_H
#define CIRCLE_H

// we can declare and add to the same namespace in multiple locations/files.
// this is what happens for the std namespace; otherwise, all the std namespace functions would have to be declared in the same header file.

namespace BasicMath
{
    constexpr double pi{ 3.14 };
}

#endif